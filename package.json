{
  "name": "divancosaas-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "DivancoSaaS - Multi-tenant SaaS Platform",
  "workspaces": [
    "backend",
    "web",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:shared": "npm run build --workspace=shared",
    "build:backend": "npm run build --workspace=backend",
    "build:web": "npm run build --workspace=web",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:web": "npm run dev --workspace=web"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "saas",
    "multitenant",
    "monorepo"
  ],
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "cloudinary": "^2.10.0",
    "nodemailer": "^8.0.7"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0"
  }
}
